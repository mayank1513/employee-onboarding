import { Typography } from "@mui/material";

const Header = () => {
  return (
    <header className="header">
      <Typography component="h1">Welcome to Employee Onboarding</Typography>
    </header>
  );
};

export default Header;
